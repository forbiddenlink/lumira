# Minimal requirements for Railway/Vercel deployment (Gallery-only mode)
# No ML libraries needed - just web server
#
# This is a deliberate subset of pyproject.toml's [project.dependencies]
# (web/API packages only, no ML stack). Version floors/ceilings here should
# still match pyproject.toml for any package listed in both — see issue #59.

fastapi>=0.115.0,<0.142.0
uvicorn[standard]>=0.32.0,<0.53.0
python-multipart>=0.0.31,<0.1.0
pydantic>=2.10.0,<3.0.0
pydantic-settings>=2.14.2,<3.0.0
mangum>=0.19.0,<1.0.0
aiofiles>=25.1.0
slowapi>=0.1.9,<0.2.0

# Optional: for structured logging
structlog>=24.4.0,<27.0.0
psutil>=5.9.0
