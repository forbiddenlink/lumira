# Lightweight "gallery + cloud-generation" deployment (Railway/Vercel).
# NO local ML stack (torch/diffusers/transformers/accelerate/peft): the web
# app boots torch-free and image generation runs via the cloud Replicate API.
# See Dockerfile.gallery. Keep in sync with the web boot chain, not pyproject.

# Web server + API
fastapi>=0.115.0,<0.141.0
uvicorn[standard]>=0.32.0,<0.53.0
python-multipart>=0.0.31,<0.1.0
pydantic>=2.10.0,<3.0.0
pydantic-settings>=2.14.2,<3.0.0
aiofiles>=25.1.0
slowapi>=0.1.9,<0.2.0
websockets>=12.0.0
mangum>=0.19.0,<1.0.0
jinja2>=3.1.6

# Core runtime (no ML)
sqlalchemy>=2.0.0
alembic>=1.13.0
pyyaml>=6.0.0
python-dotenv>=1.2.2
apscheduler>=3.10.0
structlog>=24.4.0,<27.0.0
tqdm>=4.66.0
httpx>=0.27.0
tenacity>=8.2.0
pillow>=12.3.0
numpy>=2.4.6
opencv-python-headless>=4.13.0.90

# Cloud image generation (no local GPU needed)
replicate>=1.0.0
